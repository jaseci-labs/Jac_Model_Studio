# JMS bug sweep — 2026-09-11

A code audit, a restructure, and a live end-to-end run of Jac ML Studio.
Every fix below went in with a test that failed on the old code, unless the
fix notes say otherwise.

## Summary

| | |
|---|---|
| Tests | 131 → **163** (19 annexes), all passing (`jac test server/<pkg>/<mod>.test.jac` from the repo root); `remote/test_supervisor.py` 5/5 |
| Audit fixes | 18 commits: 4 high, 5 medium, 9 low or robustness |
| Known issues | 5 of 6 fixed ([below](#known-issues-fixed-later-the-same-day)); 1 not possible |
| Live end-to-end | 3 more bugs found and fixed ([below](#live-run-gpu)); a few model steps still pending |
| Startup fix | cold compile cache clamped the fd limit, so the dev server died with `Errno 24` |
| Restructure | `jms/` flattened to the repo root, then split into `server/{core,models,jms,cloud}`, `client/`, `scripts/` |
| Dead code | ~786 lines removed (unreferenced components, endpoints, CSS, stubs) |

## High severity

| # | Bug | Where | Fix | Commit |
|---|---|---|---|---|
| 1 | Two users naming a project the same got one shared directory (shared dataset and runs; either could purge the other's) | `server/jms/jms_projects.sv.jac` slug check | an existing on-disk project dir counts as taken | `2f16a0a` |
| 2 | `start_remote_run` accepted a client `dataset` path, so any readable server file (other users' keys, the user DB) could be uploaded to the caller's GPU box | `server/cloud/cloudruns.sv.jac` | public endpoint drops `dataset`; the train stage uses a trusted internal path | `4883af1` |
| 3 | `rid` path traversal in project chat: `../../<other>/runs/<rid>` or an absolute path reached another user's adapter | `server/jms/jms_chat.sv.jac` | reject traversal and absolute rids | `ac8c5e0` |
| 4 | Prod Caddyfile proxied the API to :8001, but prod serves everything on :8000, so every API call returned 502; `handle /` also missed `/assets` | `deploy/Caddyfile` | one proxy to :8000 | `22fe2f2` |

## Medium

| # | Bug | Fix | Commit |
|---|---|---|---|
| 5 | `jobs.safe("..")` returned true, so a cloud run named `..` wrote into the shared `users/` dir | reject `.` and `..` | `07c3d7c` |
| 6 | `JAC_LOCAL_USER=0` turned on passwordless local mode | only 1/true/yes/on enable it | `973b0b2` |
| 7 | GPU-lock race: a second train/eval/gen could start between claiming the lock and writing the job file | the claim holds for 60s until the job file appears | `fec1fd0` |
| 8 | One NaN loss row made cloud-run status and list endpoints return 500 | drop or null NaN and Infinity in `read_series` / `last_row` | `fa4969f` |
| 9 | A stored API key that no longer decrypts (master key changed) made key-status endpoints return 500 | resolves to blank | `8c5c326` |

## Low and robustness

| # | Bug | Fix | Commit |
|---|---|---|---|
| 10 | A non-object JSON row made `add_examples` return 500; a bare string was appended as a row | type-check rows | `bf7a7bb` |
| 11 | A curate edit with non-object messages returned 500 | type-check before coercing | `eaaa1e6` |
| 12 | `restore_graph.sh` missed a running `jac start --dev` | match any `jac start` whose cwd is this checkout; stage before swapping | `bfc73ea` |
| 13 | Backups tarred live SQLite files (torn snapshots) and skipped `data/` | Python online backup API for `.db` files; `data/` included as `runtime/`; atomic write; old tarballs still restore | `bfc73ea` |
| 14 | `max_tokens` had no cap | clamped to 1–4096 server-side (4096 = chat slider max) | `c56a68f` |
| 15 | Chat, plan drafts and `warm_model` ignored the GPU lock | refuse with "GPU busy: X is running" while a heavy job holds the slot | `682cce3` |
| 16 | JMS jobs weren't counted in the per-user job cap | counted; jobs whose process is dead no longer count | `d280bd9` |
| 17 | Remote resume truncated `train.log`, which broke the log mirror and duplicated metrics rows | append on relaunch; dedupe by step | `5b2e6fd` |
| 18 | The remote run dir was keyed by name only, so re-running a name reused the old dir, skipped the new upload and pulled the old adapter | dir includes 8 characters of the run id | `6776065` |

## Startup: cold-cache fd clamp

- **Symptom.** The first `./start.sh` after the restructure died with
  `HMR initialization failed: [Errno 24] Too many open files` and
  `Could not start Vite dev server`.
- **Cause.** Traced with a `setrlimit` stack hook. On a cold `.jac/cache`,
  jaclang's module resolver imports `mlx_lm` while compiling `models.sv.jac`,
  before `inference.sv.jac`'s own guard exists. `mlx_lm/utils.py:36` then sets
  `RLIMIT_NOFILE` to `(2048, 4096)`, and a hard limit can't be raised again. The
  dev watcher needs about 13k fds.
- **Fix.** The same monotonic `setrlimit` guard now also lives in
  `scripts/pysite/sitecustomize.py`, which runs at interpreter start.
  `inference.sv.jac` detects it by name and skips its own copy.
- **Verified.** Importing `mlx_lm` without the guard gives (2048, 4096); with it
  the limit stays (65536, unlimited). A cold-cache boot came up in 22s, holding
  13,191 fds. Commit `f9f1c6b`.

## Known issues (fixed later the same day)

| # | Issue | Fix | Commit |
|---|---|---|---|
| 19 | A heavy job (train/eval/gen) could start while a chat model was resident, putting two 16GB copies on a 48GB box | `jobs.heavy_acquire` evicts the resident model once it holds the slot. If a chat stream holds the model lock, the eviction is marked pending and the stream stops at its next token, then unloads. Chat and warm already refuse while `heavy_busy()` | `92bbdc7` |
| 20 | `jms_chat` was authorized by knowing the project id | caller must own the project (checked against their own graph before streaming) | `bd05eab` |
| 21 | Project sources could be any readable directory | local mode unchanged; in multi-user mode the resolved (realpath) source must be under `JAC_STUDIO_SOURCE_ROOTS`, or by default the caller's own project dirs. Symlink escapes and look-alike siblings are rejected, and `scan_source` rechecks | `1bb9b6c` |
| 22 | Unused `clsx` / `tailwind-merge` in `jac.toml` | removed (nothing imports them) | `7d8dd05` |
| 23 | Unreachable `SpheronAPI` path in `remote/supervisor.py` | removed with the `--no-spheron-api` plumbing on both sides; BYO-cluster behaviour unchanged, 4 supervisor tests added | `b00b48f` |
| — | Prod binds 0.0.0.0 | **not fixable here**: `jac start` has no host option (no flag, env or working `jac.toml` key). The earlier Caddyfile note claiming `[plugins.scale.server] host` works was wrong and is corrected. Firewall ports 8000/8001 in prod | `2e70f96` (docs) |

Merge note: the live run below had independently added a second
`inference.evict()` plus a direct call after each `heavy_acquire` in
`jms_gen`/`jms_train`/`jms_eval` (`7321b2b`). The merge (`def8cc5`) keeps the
`heavy_acquire` version above, drops the duplicate `def` and the redundant
calls, and rewrites the `inference.test.jac` eviction tests for it.

### Still open

- Project sources in local mode stay unrestricted (single user, by design).
- Tests share the project's persisted graph (`.jac/data`, by Jac design).
  Running them while the dev server is up can flake. Run them with the server down.
- Editing a server file under `jac start --dev` can serve a client bundle
  missing its `app` export until a restart. That's in jaclang, not this repo.

## Live run (GPU)

A real browser (Playwright headless) against the dev server, on the merged
code, with the rules below.

| Area | Result |
|---|---|
| Crawl | every page, drawer, modal, command palette, shortcut sheet and all five project stages, light + dark: **0 console errors, 0 failed requests, 0 unlabeled inputs** |
| DATA | all 4 dataset files page correctly (12,570 / 2,217 / 1,169 / 206 rows); save-example rejects bad JSON, adds valid rows |
| CLOUD | bad key name, garbage link, bad port and unreachable host each give a clean error, with no hang; the test cluster and key were deleted afterwards |
| Chat | base model streamed at 45 tok/s. Switching to the 08 adapter took 22s to load, then it streamed at 17.5 tok/s |
| Assistant | answers from real data; navigation works; settings change is confirm-gated and saved; no-key and bad-key messages are clean |
| JMS pipeline | project + source added, bogus source path rejected, missing-API-key hint shown, local MLX plan draft (28s), plan approved |

Bugs found and fixed in the live run:

| # | Bug | Cause | Fix | Commit |
|---|---|---|---|---|
| 24 | Deleting a project from the UI wiped its whole folder, trained adapters included | the UI omits `purge_files`, and jac-scale fills the default in as the **string** `"False"`, which was truthy | only a real `True` purges; test added; checked in the browser | `513dcaf` |
| 25 | Nothing unloaded the server's model before an MLX gen/train/eval worker loaded its own | missing eviction | superseded by #19 at merge | `7321b2b` |
| 26 | Escape didn't close the Experiments Chat model picker or COMPARE popover | their page-wide backdrop only handled clicks | Escape closes both; checked in the browser | `5cb91ee` |

**Pending** (blocked while another session trains experiment 09 on the GPU,
about 3 hours; the dev server is stopped for that run):

- synthesize rows → curate/split → tiny local MLX train → eval → chat with
  the trained adapter (also the first live check that #19 fires)
- two-user prod/login isolation check
- delete the leftover `e2e-test-proj` (graph record + `data/projects/` folder)
  through the app with `purge_files=true`
- restart the studio (`./start.sh`)

### GPU sharing incident

An end-to-end agent loaded a second 16GB model while another session's eval
held the GPU, and kept restarting the dev server, loading a model each time.
Swap hit 41GB and two processes were stuck in the kernel (`?E`) for 40 minutes
before clearing. Rules since then:

- one MLX model on the machine at a time, via `/tmp/jac-gpu.lock` with an owner file
- no automatic server restarts
- one headless browser at a time
- serial `jac` runs when memory is tight

Fix #19 now also makes the app itself unload its chat model before a heavy job.
